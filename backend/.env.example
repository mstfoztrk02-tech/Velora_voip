# ============================================
# VELORA VOIP BACKEND - ENVIRONMENT VARIABLES
# ============================================

# MongoDB Configuration
MONGO_URL=mongodb://localhost:27017
DB_NAME=velora_voip

# MM (number add) integration
# Remote endpoint called by backend proxy: POST /api/mm/add_numbers_to_call
MM_API_URL=http://185.8.12.117:81/_api/add_numbers_to_call.php
MM_API_KEY=

# CORS Configuration
CORS_ORIGINS=http://localhost:3000,https://your-frontend-domain.com

# API Keys (if needed for backend integrations)
# Note: Most API keys should be in frontend .env for Vercel serverless functions

# Logging Level
LOG_LEVEL=INFO

# Server Configuration
HOST=0.0.0.0
PORT=8000
